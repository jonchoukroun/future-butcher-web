{{stats-bar/dash-board sendQuit=(action "sendToScores")}}

<div class="container-fluid top-spacer">

  <div class="row justify-content-center mb-3">
    <div class="col-12 col-sm-10 col-md-6 mt-1 mt-md-4">

      {{#if (eq currentStation "bell_gardens")}}
        {{store/store-interface}}

      {{else}}
        {{market/meat-market}}
      {{/if}}

    </div>
  </div>
</div>

{{nav-bar}}
