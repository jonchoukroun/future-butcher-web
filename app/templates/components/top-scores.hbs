<div class="container mt-3">
  <div class="row justify-content-center px-0">
    <div class="col-12 col-sm-10 col-md-6">
      <span class="border-bottom border-secondary mb-4">
        <h2 class="end-text text-center">The End</h2>
      </span>

      {{#if isHighestScore}}
        <h5 class="text-center">You set a new record!</h5>

        <h3 class="mx-5 text-center">
          {{format-money playerScore precision=0}}
        </h3>

        <div class="m-4 py-3 border-top border-black">
          <p>
            You made enough money to leave behind crumbling LA and retire in luxury.
          </p>
        </div>

        <div class="my-2 mx-1 py-3 container">
          <div class="row justify-content-center">
            <h5>Runners up</h5>
          </div>

          {{#each lowerScores as |score|}}
            <div class="row justify-content-around">
              <div class="col-2 text-right">
                <h6 class="m-1">{{score.rank}}</h6>
              </div>

              <div class="col-6 px-0 text-left">
                <h6 class="m-1">{{score.player}}</h6>
              </div>

              <div class="col-4 pl-0 text-left">
                <h6 class="m-1">{{format-money score.score precision=0}}</h6>
              </div>
            </div>
          {{/each}}
        </div>

      {{else}}
        <div class="m-2 py-3 border-bottom border-black text-center">
          <h6 class="text-secondary">Reigning Champ:</h6>

          <h5 class="m1">
            {{highestScore.player}}:
            {{format-money highestScore.score precision=0}}
          </h5>
        </div>

        <div class="my-2 py-3 px-0 container">
          {{#if (gt playerScoreIndex 0)}}
            {{#each higherScores as |score|}}
              <div class="row justify-content-around">
                <div class="col-2 text-right">
                  <h6 class="m-1">
                    {{score.rank}}.
                  </h6>
                </div>

                <div class="col-6 px-0 text-left">
                  <h6 class="m-1">
                    {{score.player}}
                  </h6>
                </div>

                <div class="col-4 pl-0 text-left">
                  <h6 class="m-1">
                    {{format-money score.score precision=0}}
                  </h6>
                </div>

              </div>

            {{/each}}

            <div class="row justify-content-center bg-secondary my-1 py-1">
              <h6 class="m-1 text-black">
                <b>
                  You: {{format-money playerScore precision=0}}
                </b>
              </h6>
            </div>

            {{#each lowerScores as |score|}}
              <div class="row justify-content-around">
                <div class="col-2 text-right">
                  <h6 class="m-1">
                    {{score.rank}}.
                  </h6>
                </div>
                <div class="col-6 px-0 text-left">
                  <h6 class="m-1">
                    {{score.player}}
                  </h6>
                </div>
                <div class="col-4 pl-0 text-left">
                  <h6 class="m-1">
                    {{format-money score.score precision=0}}
                  </h6>
                </div>
              </div>

            {{/each}}

            <div class="mt-4 py-3 border-top border-black">
              <p class="text-center">
                You have enough to bribe the <span class="font-italic">coyote</span>, can you do even better next time?
              </p>
            </div>

          {{else}}
            <h6 class="text-secondary text-center">
              Your score:
            </h6>

            <h6 class="text-center">
              {{format-money playerScore precision=0}}
            </h6>

            <p>
              Terrible, just terrible. You didn't make enough to bribe the <span class="font-italic">coyote</span> and you're stuck in LA. Give it another shot, before someone harvests your flesh.
            </p>

          {{/if}}
        </div>

      {{/if}}
    </div>
  </div>
</div>
